# TempoTasks Design System

## 1. 视觉主题与氛围

TempoTasks 是一件深色、精密、键盘优先的 macOS 工具。界面像夜间使用的专业指令台：安静、紧凑、立即响应。功能永远先于装饰。

## 2. 色板与角色

- Canvas：`#07080A`
- Primary surface：`#101111`
- Raised surface：`#1B1C1E`
- Primary text：`rgba(255,255,255,0.88)`
- Secondary text：`rgba(255,255,255,0.64)`
- Disabled/completed text：`rgba(255,255,255,0.38)`
- Hairline：`rgba(255,255,255,0.06)`
- Strong border：`rgba(255,255,255,0.10)`
- Focus blue：`#55B3FF`
- Completion coral：`#FF6363`
- Error coral：`#FF6363`
- Success green：`#5FC992`

蓝色只表示交互焦点和当前日期；珊瑚红只表示完成与错误，不互换职责。

## 3. 排版规则

- 中文与正文：SF Pro / PingFang SC 系统字体，正文 14–15 pt、Regular/Medium。
- 标题：系统字体 20–24 pt、Semibold，不使用负字距压缩中文。
- 日期数字与快捷键：SF Mono 10–20 pt，启用等宽数字。
- 中文不使用斜体；正文行高不低于 1.5。

## 4. 组件样式

- 输入框：8 pt 圆角，深色底，静态 hairline；聚焦时 1 pt 蓝边 + 3 pt 低透明蓝环。
- 日期项：10 pt 圆角。选中态使用整块蓝色低透明底 + 全边框，不使用左侧竖线。
- 任务行：使用分隔线而不是卡片；checkbox 为 18 pt、6 pt 圆角。
- 完成态：checkbox 珊瑚红填充；整行文字置灰，标题加 1 pt 删除线。
- 删除按钮：悬停任务行时显现，始终提供辅助功能名称。
- 撤销提示：10 pt 圆角的 raised surface，右侧文字按钮。

## 5. 布局原则

- 悬浮面板默认 760 × 520 pt。
- 左侧日期轨道宽 164 pt；右侧内容自适应。
- 面板内部基于 4 pt 网格，主要间距使用 8/12/16/24 pt。
- 任务列表是唯一滚动区；输入框和日期轨道保持稳定。
- 一个屏幕只有一个主视觉焦点：快速输入框。

## 6. 深度层级

- Level 0：Canvas，无阴影。
- Level 1：Surface，以 6% 白色 hairline 分层。
- Level 2：输入框与选中日期，外环 + 内部 1 pt 高光。
- Level 3：悬浮面板与撤销提示，使用染色双层阴影；不使用外发光。

## 7. Do / Don’t

### Do

- 让 `Control + Space`、回车和 Escape 形成稳定肌肉记忆。
- 在高频动作上立即响应。
- 用完整底色、边框和字重表达选中态。
- 保留空状态、错误状态、快捷键冲突和超长标题恢复路径。

### Don’t

- 不使用纯黑、紫蓝渐变、霓虹和玻璃拟态堆叠。
- 不在列表项左侧使用竖线强调。
- 不用卡片包裹每个任务。
- 不为快捷键唤出和键盘提交添加动画。
- 不使用第三方字体或图标库。

## 8. 响应与可访问性

- 面板在当前屏幕顶部居中，并限制在可见区域。
- 窄屏时日期轨道压缩到 144 pt，不隐藏今天和明天。
- 所有图标按钮有 VoiceOver 名称，所有交互有可见焦点。
- 文字对比度以暗色中文界面 7:1 为目标。
- 尊重“减少动态效果”；状态变化仍保持可识别。

## 9. Motion 哲学

- `Control + Space` 唤出、隐藏：0ms，立即完成。
- checkbox：只做 100–120ms 的颜色/透明度反馈，不位移。
- hover 删除按钮：100ms 透明度反馈。
- 撤销提示：160ms ease-out 出现，120ms 退出；减少动态效果时只改变透明度。
- 不使用弹簧、stagger、scale(0) 或超过 200ms 的常规 UI 动效。

## 10. 工艺密度清单

1. 低于 3% 的冷蓝环境色层。
2. 品牌化蓝色焦点环。
3. 悬停揭示删除操作。
4. 外环 + 内高光的多层染色阴影。
5. 等宽快捷键帽与表格数字。
6. 细 hairline 和精确 4 pt 网格。
7. 面板、输入框、日期项三层材质层级。
