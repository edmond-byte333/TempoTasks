# TempoTasks Design System

## 1. 视觉主题与氛围

锚点是**夜间飞行仪表盘**。使用场景是晚上十一点、房间没开灯、屏幕是唯一光源。深色不是审美偏好，是物理要求：不破坏暗适应。

从仪表盘借三件事，不是借它的样子：

1. **亮度即优先级。** 仪表盘用亮度区分扫视顺序，不用颜色编码一切。
2. **航空语义色序。** 绿表示正常与当前，琥珀表示注意，红只留给告警。
3. **分组靠留白，不靠线框。**

功能永远先于装饰。

## 2. 色板与角色

全部用 OKLCH 定义，实现见 `TempoTheme.swift` 里的 `oklch()`。OKLCH 在感知上均匀，等量的亮度变化看起来才是等量的。

色相锚在 **165（青绿）**，所有中性色统一微偏这个色相，避免出现死灰。选 165 有两个理由：避开了蓝（250）和暖橙（60）这两个最常见的默认色相；同时对应仪表盘里"正常 / 当前"的绿。

### 表面（暗色下的深度来自亮度，不是阴影）

| Token | OKLCH | 用途 |
|---|---|---|
| `canvas` | 14.5% 0.004 165 | 主内容区底 |
| `surface` | 18% 0.005 165 | 左栏、未聚焦输入框 |
| `raised` | 23.5% 0.006 165 | 键帽、撤销条 |
| `rowHover` | 20.5% 0.005 165 | 行悬停 |

### 文字（四级亮度就是四级优先级，这是整套设计的主线）

| Token | OKLCH | 用途 |
|---|---|---|
| `textUrgent` | 95% 0.005 165 | 欠了 7 天以上 |
| `textPrimary` | 86% 0.006 165 | 常规、欠 1 到 6 天 |
| `textSecondary` | 62% 0.008 165 | 次要信息、未安排任务 |
| `textDim` | 44% 0.006 165 | 已完成、分组标签 |

### 语义色

| Token | OKLCH | 职责 |
|---|---|---|
| `accent` | 76% 0.13 165 | 焦点与当前选中。界面里唯一常驻的彩色，占比控制在 10% 以内 |
| `caution` | 80% 0.12 75 | 注意级。只在欠了 7 天以上时作为亮度之外的第二信号 |
| `alert` | 68% 0.19 25 | 告警级。只给真正的错误 |

**职责不互换。** 特别是：`alert` 不再用于表示"完成"。完成的事应该退场，不该是画面里最重的颜色。

## 3. 排版规则

- 中文与正文：SF Pro / PingFang SC 系统字体，正文 14–15 pt。
- 标题：24 pt Semibold，不使用负字距压缩中文。
- **等宽只给日期数字、计数和快捷键帽。** 中文绝不使用等宽：方块字在等宽下字距会被强行拉散。需要数字对齐时用 `monospacedDigit()`，不要整段设 `design: .monospaced`。
- 层级至少同时用两个维度（字号 + 字重、或亮度 + 字重），不靠单一维度。
- 中文不使用斜体；正文行高不低于 1.5。

## 4. 组件样式

- **输入框**：8 pt 圆角。未聚焦时 `surface` 底 + hairline；聚焦时升到 `raised` 底 + 1 pt accent 边 + 3 pt 低透明外环。它是屏幕上唯一的主焦点。
- **左栏条目**：10 pt 圆角，44 pt 高。选中态用整块低透明 accent 底，不使用左侧竖线。
- **待办徽章**：胶囊形。选中时 accent 底 + 深色数字，未选中时 `raised` 底。
- **任务行**：**不使用分隔线，也不使用卡片**，靠行距和悬停底色分隔。
- **checkbox**：18 pt、6 pt 圆角。完成态填 `textDim`，勾用 `canvas` 色，整行文字降到 `textDim` 并加删除线。
- **逾期标记**：等宽的 `MM/DD` 原定日期，7 天以上用 `caution`，以内用 `textDim`。显示绝对日期而非相对天数，紧迫感由标题亮度承担，不重复表达。VoiceOver 仍然读出逾期天数。
- **分组标题**：`textDim`，10 pt，轻微字距。像仪表盘上的刻度标签。带批量操作时，操作入口放在标题行右端。
- **批量操作入口**：文字按钮，默认 `textDim` 无底色，悬停才升到 `textPrimary` 加 `raised` 胶囊底。破坏性不高但影响面大的操作要低调，避免误点。
- **归档视图**：只读。不显示输入框、不显示完成度、不提供改日期入口，行尾显示完成日期。

## 5. 布局原则

- 悬浮面板 760 × 520 pt。
- 左侧轨道 168 pt，与右侧靠**背景明度差**分隔，不用 1 pt 线。
- 面板内部基于 4 pt 网格，主要间距 8/12/16/24/32 pt。间距要有变化，处处相同即是单调。
- 任务列表是唯一滚动区；输入框和左栏保持稳定。
- 左栏自上而下是使用频率的顺序：待办置顶，日程居中，归档沉底。「已完成」是偶尔回看的东西，不与主流程抢位置。
- 待办内分两组：「还没安排」在前，新记下的落在这里，回车后不必滚动就能看到；「待处理」在后，组内按日期从近到远，刚过期的往往还救得回来。
- 一个屏幕只有一个主视觉焦点：快速输入框。
- 空状态靠上放，紧跟输入框，不垂直居中。居中会让它漂到面板下半部，离刚打完字的地方太远。

## 6. 深度层级

暗色界面里深度来自表面亮度，不是阴影。三级台阶：`canvas` → `surface` → `raised`。悬浮面板本身有外阴影，面板内部不再叠加阴影。

## 7. Do / Don't

### Do

- 让唤出快捷键（默认 `Control + Command + Space`，用户可改）、回车和 Escape 形成稳定肌肉记忆。
- 用亮度表达时间压力：欠得越久越亮。
- 在高频动作上立即响应。
- 保留空状态、错误状态、快捷键冲突和超长标题的恢复路径。
- 状态至少有颜色之外的第二信号（位置、字重、删除线）。

### Don't

- 不用纯黑纯白；所有中性色都带 0.004–0.008 的色度。
- 不用红色表达"完成"或"逾期"。红色只给错误。
- 不在列表项左侧使用竖线强调。
- 不用卡片包裹每个任务，不用分隔线切割列表。
- 不给中文设等宽字体。
- 不为快捷键唤出和键盘提交添加动画。
- 不使用玻璃拟态、渐变文字、霓虹和第三方图标库。

## 8. 响应与可访问性

- 面板在当前屏幕顶部居中，并限制在可见区域。
- 所有图标按钮有 VoiceOver 名称，所有交互有可见焦点。
- 深色界面下中文正文对比度目标 7:1。
- 尊重"减少动态效果"；关闭动效后状态变化仍然可辨认。
- 逾期状态同时由亮度、字重和文字标记表达，不只靠颜色。

## 9. Motion 哲学

- 快捷键唤出、隐藏：0 ms，立即完成。
- 悬停、聚焦、勾选：110 ms 的颜色与透明度反馈，不位移。
- 撤销提示：160 ms ease-out 出现。
- 不使用弹簧、stagger、scale(0) 或超过 200 ms 的常规 UI 动效。

## 10. 工艺密度清单

1. OKLCH 定义的色板，中性色统一微偏 165。
2. 四级文字亮度对应四级优先级。
3. 逾期天数映射到亮度与字重，而不是映射到红色。
4. 悬停才显现的行内操作。
5. 等宽数字与快捷键帽，中文保持常规字体。
6. 精确 4 pt 网格，间距有节奏变化。
7. 三层表面材质，靠亮度而非阴影表达深度。
8. 分组靠留白，列表无分隔线。
